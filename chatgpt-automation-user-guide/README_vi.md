[![Tải tại đây](https://img.shields.io/badge/⬇_Tải-Tại_Đây-success?style=for-the-badge)](https://chromewebstore.google.com/detail/chatgpt-automation-auto-c/nocgcjgldlpeffhdhfjejhcgjbgcmpgb)

# 🚀 ChatGPT Automation v1.0.2 - Tự động hóa AI trên chatgpt.com [![English](https://img.shields.io/badge/English-blue)](README.md) [![中文](https://img.shields.io/badge/中文-red)](README_zh.md)

**ChatGPT Automation** là một công cụ năng suất mạnh mẽ được thiết kế để tăng cường quy trình làm việc của bạn trên ChatGPT.com. Dừng việc nhập từng prompt một cách thủ công—hãy tự động hóa quy trình và tạo phản hồi cùng hình ảnh hàng loạt ở quy mô lớn.

-----

## ✨ Các tính năng chính

* **🚀 Xử lý hàng loạt nâng cao:** Xếp hàng hàng chục hoặc hàng trăm prompt và để tiện ích tự động xử lý việc gửi và tạo nội dung.
* **💬 Tự động hóa Xử lý Văn bản:** Tạo hàng loạt phản hồi văn bản từ ChatGPT. Hỗ trợ nhiều prompt với thời gian chờ tùy chỉnh.
* **🧩 Thành phần thành Văn bản:** Xử lý nhiều hình ảnh hoặc thành phần đầu vào cùng với mỗi prompt.
* **🖼️ Tạo hàng loạt Văn bản thành Hình ảnh:** Tạo nhiều hình ảnh đồng thời từ mô tả văn bản.
* **🔄 Hình ảnh thành Hình ảnh:** Biến đổi và nâng cao hình ảnh hiện có bằng AI với gợi ý văn bản.
* **⚙️ Bộ điều khiển chuyên nghiệp:**
    * **Prompt đồng thời:** Xử lý nhiều prompt cùng lúc để tiết kiệm thời gian.
    * **Chờ thông minh (Smart Delays):** Thiết lập khoảng thời gian tùy chỉnh giữa các prompt để quản lý giới hạn tốc độ hiệu quả.
    * **Tự động tải xuống:** Tự động lưu kết quả vào máy tính của bạn.
    * **Tự động đổi tên tệp:** Tự động đổi tên các tệp đã tải xuống để giữ chúng ngăn nắp.
    * **Lưu vào thư mục:** Chỉ định thư mục con tùy chỉnh cho các tệp tải xuống theo từng dự án.
* **🎭 Tự động thêm ảnh nhân vật:** Tự động ghép và đính kèm ảnh vào prompt dựa trên tên nhân vật trong tên tệp.
* **📊 Quản lý hàng đợi thời gian thực:** Theo dõi tiến trình tạo của bạn với thanh trạng thái trực quan và danh sách prompt hoạt động trong Bảng bên (Side Panel).
* **📂 Quản lý tệp ngăn nắp:** Tự động sắp xếp các bản tải xuống vào các thư mục riêng theo dự án.
* **🔧 Sửa lỗi nhanh:** Công cụ một chạm để khắc phục lỗi tạo nội dung.
* **💳 Quản lý gói:** Đăng nhập để kiểm tra gói của bạn và theo dõi lượng prompt sử dụng hàng ngày.
* **🌐 Hỗ trợ đa ngôn ngữ:** Có sẵn tiếng Anh, tiếng Việt, tiếng Trung, tiếng Hàn, tiếng Tây Ban Nha và tiếng Nhật.

-----

## 📥 Cài đặt

### Cách 1: Cửa hàng Chrome trực tuyến (Khuyên dùng)
1. Truy cập [Cửa hàng Chrome trực tuyến](https://chromewebstore.google.com/detail/chatgpt-automation-auto-c/nocgcjgldlpeffhdhfjejhcgjbgcmpgb)
2. Nhấn **Thêm vào Chrome**.

---

## 📖 Hướng dẫn sử dụng

### Bắt đầu

1. **Truy cập ChatGPT**
   - Mở [chatgpt.com](https://chatgpt.com)
   - Tiện ích chỉ hoạt động trên các trang ChatGPT.

2. **Mở tiện ích**
   - Nhấn vào biểu tượng tiện ích trên thanh công cụ Chrome. Hãy ghim nó để truy cập dễ dàng hơn!

3. **Cấu hình Cài đặt Hàng loạt**
   - Trong tab **Điều khiển**, bạn có thể thiết lập:
     - **Prompt đồng thời:** Số lượng prompt chạy cùng một lúc.
     - **Thời gian chờ prompt:** Thời gian nghỉ giữa mỗi lần gửi prompt.
     - **Lưu vào thư mục:** Tên thư mục con cho các tệp tải xuống.
     - **Tự động đổi tên tệp:** Bật/tắt tự động đổi tên tệp.

4. **Chọn một chế độ**
   - Chọn từ các chế độ có sẵn: **Xử lý Văn bản**, **Thành phần thành Văn bản**, **Văn bản thành Hình ảnh**, hoặc **Hình ảnh thành Hình ảnh**.

---

### 1. Chế độ Xử lý Văn bản

1. Chọn chế độ **Xử lý Văn bản**.
2. Nhập các prompt vào ô nhập liệu (tách mỗi prompt bằng một **dòng trống**).
3. Hoặc nhấn **hoặc tải lên tệp .txt** để nhập danh sách prompt từ file `.txt`.
4. Nhấn **Chạy** để bắt đầu xử lý hàng loạt.

**Ví dụ Prompt:**
```
Viết một email chuyên nghiệp gửi khách hàng về việc trì hoãn dự án.
Hãy giữ nội dung ngắn gọn và xin lỗi.

Tóm tắt các lợi ích chính của làm việc từ xa trong 5 gạch đầu dòng.

Viết mô tả sản phẩm cho tai nghe chống ồn.
Đối tượng mục tiêu: nhân viên làm việc từ xa.
```

---

### 2. Chế độ Thành phần thành Văn bản

1. Chọn chế độ **Thành phần thành Văn bản**.
2. Tải lên các hình ảnh hoặc tệp thành phần bạn muốn đưa vào.
3. Nhập các prompt (tách bằng dòng trống). Hình ảnh sẽ được xử lý cùng với mỗi prompt.
4. Tùy chọn bật **Tự động thêm ảnh nhân vật** để tự động đính kèm ảnh có tên tệp khớp với tên nhân vật trong prompt.
5. Trong **Cài đặt**, đặt **Số ảnh đầu vào tối đa mỗi Prompt** (1–3) để kiểm soát số lượng ảnh dùng cho mỗi prompt.
6. Nhấn **Chạy**.

---

### 3. Chế độ Văn bản thành Hình ảnh

1. Chọn chế độ **Văn bản thành Hình ảnh**.
2. Nhập mô tả chi tiết cho hình ảnh (tách mỗi prompt bằng một **dòng trống**).
3. Cấu hình **Mô hình hình ảnh** mong muốn trong tab Cài đặt.
4. Nhấn **Chạy**.

**Ví dụ Prompt:**
```
Cảnh núi yên bình lúc bình minh với ánh sáng vàng phản chiếu trên hồ.

Đường chân trời thành phố tương lai vào ban đêm với đèn neon và xe bay.

Nội thất quán cà phê ấm cúng với ánh sáng vàng và kệ sách trên tường.
```

---

### 4. Chế độ Hình ảnh thành Hình ảnh

1. Chọn chế độ **Hình ảnh thành Hình ảnh**.
2. Tải lên các ảnh nguồn bạn muốn biến đổi.
3. Nhập các prompt (tách bằng dòng trống).
4. Tùy chọn bật **Tự động thêm ảnh nhân vật** để tự động đính kèm ảnh dựa trên tên tệp.
5. Trong **Cài đặt**, đặt **Số ảnh đầu vào tối đa mỗi Prompt** (1–10).
6. Nhấn **Chạy**.

---

## ⚙️ Cấu hình Cài đặt

Truy cập tab **Cài đặt** để tùy chỉnh trải nghiệm của bạn:

| Cài đặt | Mô tả |
| :--- | :--- |
| **Chế độ mặc định** | Thiết lập chế độ nào sẽ được mở mặc định. |
| **Số đầu ra mỗi Prompt (Văn bản)** | Thiết lập số lượng phản hồi văn bản (1–4) tạo ra cho mỗi prompt. |
| **Số ảnh đầu ra mỗi Prompt** | Thiết lập số lượng hình ảnh (1–50) tạo ra cho mỗi prompt. |
| **Prompt đồng thời** | Số lượng prompt xử lý cùng một lúc (1–6). |
| **Độ trễ ngẫu nhiên** | Thời gian chờ ngẫu nhiên trước khi xử lý prompt tiếp theo. |
| **Mô hình Văn bản** | Chọn mô hình tạo văn bản để sử dụng. |
| **Mô hình Hình ảnh** | Chọn mô hình AI để tạo hình ảnh từ văn bản. |
| **Tùy chọn Chế độ Prompt mặc định** | Chế độ mặc định cho prompt: Cuộc trò chuyện mới hoặc Nối tiếp. Prompt cuối luôn dùng Cuộc trò chuyện mới. |
| **Tùy chọn Chế độ Hình ảnh mặc định** | Chế độ đầu vào mặc định cho prompt hình ảnh: Ảnh mới hoặc Ảnh cuối. |
| **Số ảnh đầu vào tối đa mỗi Prompt (Thành phần)** | Cho chế độ Thành phần thành Văn bản: 1–3 ảnh mỗi prompt. |
| **Số ảnh đầu vào tối đa mỗi Prompt (Hình ảnh thành Hình ảnh)** | Cho chế độ Hình ảnh thành Hình ảnh: 1–10 ảnh mỗi prompt. |
| **Số lần thử lại tối đa khi lỗi** | Số lần thử lại nếu thất bại (1–20). |
| **Tự động tải xuống (Văn bản)** | Tùy chọn tải xuống cho tạo văn bản: Không tải hoặc tùy chỉnh. |
| **Chất lượng tự động tải xuống (Hình ảnh)** | Không tải hoặc chất lượng 1k. |
| **Ngôn ngữ** | Chuyển đổi giữa English, Tiếng Việt, 中文, 한국어, Español, 日本語. |

---

## 💡 Mẹo & Thực hành tốt nhất

1. **Thời gian chờ:** Nếu bạn gặp giới hạn tốc độ, hãy tăng **Độ trễ ngẫu nhiên** trong Cài đặt.
2. **Chạy đồng thời:** Hãy bắt đầu với 1 prompt đồng thời và tăng dần để xem tài khoản của bạn hỗ trợ mức nào.
3. **Viết Prompt:** Hãy cụ thể! Prompt chi tiết sẽ dẫn đến kết quả AI tốt hơn. Tách nhiều prompt bằng một dòng trống rõ ràng.
4. **Quản lý tệp:** Dùng trường **Lưu vào thư mục** để giữ các tải xuống theo dự án trong thư mục con có tên.
5. **Ảnh nhân vật:** Đặt tên tệp ảnh theo tên nhân vật (vd: `hero.png`, `villain.jpg`) và bật **Tự động thêm ảnh nhân vật** để chúng được ghép tự động.
6. **Nối tiếp Prompt (Concat):** Dùng chế độ *Nối tiếp* để ghép các prompt lại với nhau — mỗi prompt được kết hợp với prompt tiếp theo trong cùng một cuộc trò chuyện ChatGPT.
7. **Ghép chuỗi ảnh (Ảnh cuối):** Dùng chế độ *Ảnh cuối* cho Hình ảnh thành Hình ảnh — đầu ra của mỗi prompt trở thành đầu vào của prompt tiếp theo.
8. **Sửa lỗi nhanh:** Nếu quá trình tạo bị kẹt hoặc lỗi, hãy nhấn **Sửa lỗi** trong tab Điều khiển.

---

## 🔧 Khắc phục sự cố

| Vấn đề | Giải pháp |
| :--- | :--- |
| **Tiện ích không hoạt động** | Đảm bảo bạn đang ở [chatgpt.com](https://chatgpt.com). Tải lại trang (Ctrl+R hoặc F5) nếu cần. |
| **Lỗi khi tạo** | ChatGPT có thể đang bận. Tiện ích sẽ tự động thử lại theo cài đặt **Số lần thử lại tối đa**. Dùng **Sửa lỗi** để khắc phục thủ công. |
| **Tải xuống không hoạt động** | Đảm bảo tùy chọn "Hỏi nơi lưu từng tệp trước khi tải xuống" đã được **TẮT** trong Cài đặt Chrome. |
| **Yêu cầu đăng nhập** | Hãy đảm bảo bạn đã đăng nhập vào tài khoản ChatGPT của mình. |
| **Lỗi kết nối** | Tải lại trang ChatGPT (Ctrl+R, F5) và thử lại. Nếu vẫn gặp lỗi, hãy thử cài đặt lại tiện ích. |

---

## 🔒 Quyền riêng tư & Dữ liệu

* **Xử lý tại chỗ:** Tất cả logic tự động hóa chạy cục bộ trong trình duyệt của bạn.
* **Không thu thập dữ liệu:** Chúng tôi không lưu trữ hoặc thu thập các prompt, hình ảnh hoặc dữ liệu tài khoản của bạn.
* **Lưu trữ an toàn:** Các cài đặt được lưu trong bộ nhớ cục bộ của trình duyệt và đồng bộ trên tất cả các tab.

---

## 📞 Hỗ trợ

- **Tác giả:** Trường Nguyễn
- **Website:** [kylenguyen.me](https://kylenguyen.me)
- **Phản hồi:** Sử dụng liên kết "Báo lỗi" trong tiện ích.

---

## 📦 Phiên bản

Phiên bản hiện tại: **1.0.2**

---

## 📜 Bản quyền

Bản quyền © 2026 **Trường Nguyễn**. Bảo lưu mọi quyền.

Phần mềm này là tài sản riêng. Việc sao chép hoặc phân phối trái phép đều bị nghiêm cấm.

---

**Được thực hiện với ❤️ bởi Trường Nguyễn**
